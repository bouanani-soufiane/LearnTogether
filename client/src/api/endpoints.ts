const apiPrefix = "/api/v1";

export const API = {
    login: `${apiPrefix}/auth/login`,
    logout: `${apiPrefix}/auth/logout`,
    register: `${apiPrefix}/auth/register`,
    allQuestions: `${apiPrefix}/questions`,
}